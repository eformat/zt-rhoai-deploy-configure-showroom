apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# RHOAI operator install (connected or from a local CatalogSource when disconnected).
# For disconnected installs, change subscription.yaml `source` to your local CatalogSource.
resources:
  - namespace.yaml
  - operatorgroup.yaml
  - subscription.yaml
