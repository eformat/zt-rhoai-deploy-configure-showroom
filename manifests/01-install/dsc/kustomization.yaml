apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# DSCInitialization must reconcile before the DataScienceCluster; apply order below is preserved.
resources:
  - dscinitialization.yaml
  - datasciencecluster.yaml
