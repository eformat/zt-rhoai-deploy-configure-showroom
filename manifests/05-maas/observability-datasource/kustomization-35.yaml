---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# RHOAI 3.5: Creates a PersesGlobalDatasource so the Usage dashboard tab (deployed in the
# redhat-ods-applications project) can resolve the data-science-prometheus-datasource by
# name. The 3.4 caCert.namespace bug is fixed in 3.5; this global datasource is still
# required for the Usage tab to render correctly.
resources:
  - perses-global-datasource.yaml
