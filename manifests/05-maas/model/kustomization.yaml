---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The LLMInferenceService MaaS will govern. Applied into the workshop project with
# `oc apply -k manifests/05-maas/model -n <project>`.
resources:
  - llminferenceservice.yaml
