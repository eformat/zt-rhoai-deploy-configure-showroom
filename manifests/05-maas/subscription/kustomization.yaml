apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Technology Preview MaaS resources. The issued API key lands in a Secret
# (maas-apikey-demo) that the module reads to call the gateway.
resources:
  - tenant.yaml
  - maasmodelref.yaml
  - maassubscription.yaml
  - maasauthpolicy.yaml
