---
# Technology Preview — MaaS custom resources. Schemas may change across releases;
# validate against your cluster's CRDs (oc explain tenant.maas...) during workshop-act.
apiVersion: maas.redhat.com/v1alpha1
kind: Tenant
metadata:
  name: demo-tenant
  namespace: rhoai-deploy
spec:
  displayName: "Demo Tenant"
